<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:skia="clr-namespace:SkiaSharp.Extended.UI.Controls;assembly=SkiaSharp.Extended.UI"
             x:Class="NeuroMate.Views.NBackGamePage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="{StaticResource PageBackground}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header z awatarem i timerem -->
        <Frame Grid.Row="0" Style="{StaticResource ElevatedCard}" Margin="20,40,20,20">
            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Awatar -->
                <Frame Grid.Column="0" Style="{StaticResource AvatarFrame}">
                    <skia:SKLottieView x:Name="NBackAvatarLottie"
                                       Source="turtle.json"
                                       RepeatCount="-1"
                                       IsAnimationEnabled="True"
                                       WidthRequest="56"
                                       HeightRequest="56" />
                </Frame>

                <StackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                    <Label Text="🔢 Test Pamięci Roboczej" Style="{StaticResource HeadlineLabel}" />
                    <Label x:Name="InstructionLabel"
                           Text="Wybierz kształt, który pojawił się jeden krok wcześniej"
                           Style="{StaticResource SubtitleLabel}" />
                </StackLayout>

                <!-- Timer -->
                <Frame Grid.Column="2" 
                       Background="{StaticResource AccentGradient}"
                       CornerRadius="20"
                       Padding="16,12"
                       HasShadow="True">
                    <Label x:Name="TimerLabel" 
                           Text="120s"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalOptions="Center" />
                </Frame>
            </Grid>
        </Frame>

        <!-- Główna zawartość gry -->
        <ScrollView Grid.Row="1" Padding="20,0">
            <StackLayout Spacing="24">

                <!-- Obszar prezentacji kształtu -->
                <Frame Style="{StaticResource ElevatedCard}" 
                       HeightRequest="200"
                       BackgroundColor="{StaticResource SurfaceVariant}">
                    <Grid>
                        <!-- Aktualny bodziec - kształt -->
                        <Label x:Name="CurrentShapeDisplay" 
                               Text=""
                               FontSize="80"
                               HorizontalOptions="Center"
                               VerticalOptions="Center"
                               IsVisible="False" />
                        
                        <!-- Komunikat oczekiwania -->
                        <Label x:Name="WaitingLabel"
                               Text="To jest kwadrat - kliknij Start aby rozpocząć"
                               FontSize="18"
                               TextColor="{StaticResource TextSecondary}"
                               HorizontalOptions="Center"
                               VerticalOptions="Center" />
                    </Grid>
                </Frame>

                <!-- Przyciski wyboru kształtów -->
                <Frame Style="{StaticResource CardFrame}">
                    <StackLayout Spacing="16">
                        <Label Text="Wybierz kształt:" 
                               Style="{StaticResource HeadlineLabel}"
                               HorizontalOptions="Center" />
                        
                        <Grid ColumnSpacing="15" RowSpacing="15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Button Grid.Row="0" Grid.Column="0"
                                    x:Name="CircleButton"
                                    Text="●"
                                    FontSize="40"
                                    Style="{StaticResource ShapeButton}"
                                    Clicked="OnShapeSelected"
                                    IsEnabled="False" />

                            <Button Grid.Row="0" Grid.Column="1"
                                    x:Name="SquareButton"
                                    Text="■"
                                    FontSize="40"
                                    Style="{StaticResource ShapeButton}"
                                    Clicked="OnShapeSelected"
                                    IsEnabled="False" />

                            <Button Grid.Row="1" Grid.Column="0"
                                    x:Name="TriangleButton"
                                    Text="▲"
                                    FontSize="40"
                                    Style="{StaticResource ShapeButton}"
                                    Clicked="OnShapeSelected"
                                    IsEnabled="False" />

                            <Button Grid.Row="1" Grid.Column="1"
                                    x:Name="DiamondButton"
                                    Text="♦"
                                    FontSize="40"
                                    Style="{StaticResource ShapeButton}"
                                    Clicked="OnShapeSelected"
                                    IsEnabled="False" />
                        </Grid>
                    </StackLayout>
                </Frame>

                <!-- Statystyki gry -->
                <Frame Style="{StaticResource CardFrame}">
                    <Grid ColumnSpacing="20" RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Label Grid.Row="0" Grid.ColumnSpan="2" 
                               Text="📊 Statystyki" 
                               Style="{StaticResource HeadlineLabel}" 
                               HorizontalOptions="Center" />

                        <StackLayout Grid.Row="1" Grid.Column="0" Spacing="4">
                            <Label Text="Próby" Style="{StaticResource StatDescription}" />
                            <Label x:Name="TrialsLabel" Text="0/30" Style="{StaticResource StatLabel}" />
                        </StackLayout>

                        <StackLayout Grid.Row="1" Grid.Column="1" Spacing="4">
                            <Label Text="Trafienia" Style="{StaticResource StatDescription}" />
                            <Label x:Name="AccuracyLabel" Text="0%" Style="{StaticResource StatLabel}" />
                        </StackLayout>
                    </Grid>
                </Frame>

            </StackLayout>
        </ScrollView>

        <!-- Kontrolki gry -->
        <Frame Grid.Row="2" Style="{StaticResource CardFrame}" Margin="20,10,20,30">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        x:Name="PauseButton"
                        Text="⏸️ Pauza"
                        Style="{StaticResource OutlineButton}"
                        Clicked="OnPauseClicked"
                        IsVisible="False" />

                <Button Grid.Column="1"
                        x:Name="StartStopButton"
                        Text="🚀 Start"
                        Style="{StaticResource PrimaryButton}"
                        Clicked="OnStartStopClicked" />

                <Button Grid.Column="2"
                        Text="❌ Wyjdź"
                        Style="{StaticResource OutlineButton}"
                        Clicked="OnExitClicked" />
            </Grid>
        </Frame>

    </Grid>
</ContentPage>
